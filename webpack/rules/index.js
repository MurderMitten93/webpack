const styleRules = require('./style.rules')
const tsRules = require('./ts.rules')
const jsRules = require('./js.rules')
const assetsRules = require('./assets.rules')
const fontRules = require('./fonts.rules')

module.exports = [
    styleRules,
    tsRules,
    jsRules,
    assetsRules,
    fontRules
]